# KIA Stick v0.4 Chat Layout Blocker Fix

Phase: `KIA-Stick-v0.4-chat-layout-blocker-fix`

Scope: fake-doc/fake-metadata-only manual QA blocker fix for chat composer overlap, bottom navigation overlap, chronological chat flow, expanded packet layout, and manifest route conflict.

## Implemented

- Moved the chat composer out of the message scroll area so the DOM order is messages, composer, then bottom nav.
- Made the message area a fixed scroll pane ending above the composer.
- Made the composer fixed above the bottom nav with a capped internal scroll height.
- Added explicit bottom separation so expanded full packet/citation content scrolls inside the message pane instead of sitting under the composer or nav.
- Kept prompt shortcuts and response options collapsed as secondary controls.
- Kept compact answer, citations, authority stack, conflicts, evidence checklist, missing facts, and follow-ups collapsed by default.
- Removed `app/manifest.ts` so `public/manifest.webmanifest` is the only manifest source.
- Added tests for chronological layout order, single manifest source, manifest JSON parse, and existing saved-answer dedupe behavior.

## Safety Boundary

- Real APWU/USPS/member/local/case/steward documents were not read, copied, listed recursively, scanned, OCRed, indexed, summarized, transformed, uploaded, ingested, or committed.
- `/media/mint/SHARED/APWU` and `~/kia-stick-private-vault` stayed out of scope.
- The implementation uses existing fake corpus metadata and deterministic local answer generation only.

## Layout Proof Targets

- Mobile 390x844: chat pane, composer, and bottom nav have separate non-overlapping rectangles.
- Desktop 1280x900: chat pane, composer, and bottom nav have separate non-overlapping rectangles.
- Expanded full packet remains inside the scrollable message pane.
- No horizontal overflow on mobile or desktop.
- Saved-answer dedupe still shows `Already saved. No new data.` and keeps saved count at 1.
- `/manifest.webmanifest` returns HTTP 200 and valid JSON from the public file.
