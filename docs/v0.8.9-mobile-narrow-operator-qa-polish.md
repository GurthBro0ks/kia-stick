# KIA-Stick-v0.8.9-mobile-narrow-operator-qa-polish

Product version: `0.7.0`
Prompt version: `prompt.fake-docs.v0.5-import-wizard-hardening`
Queue item: `queue-044-v089-mobile-narrow-operator-qa-polish`

## Scope

This subphase improves mobile/narrow operator QA support for fake-only runtime surfaces and local smoke output.

Allowed files touched: `app/globals.css`, `scripts/operator-qa-smoke.mjs`, and focused tests.

## Runtime and smoke polish

- Narrow views now wrap QA cue rails into two columns at small widths.
- Bottom nav labels keep stable dimensions and shrink at very narrow widths instead of pushing layout.
- Status pills, cue rail labels, and badges can wrap on very small screens.
- `npm run operator:smoke` now prints the bundle review surface list after static checks pass.
- The smoke helper still accepts only loopback URLs and skips route checks when no local server is running unless `--require-server` is provided.

## Bundle smoke surfaces

- Chat no-answer save blocking
- Sources citable/context labels
- Saved empty/detail/version metadata
- Upload fake metadata buttons only
- Import fake state machine
- Vault fake governance workflow
- Settings version identity
- `/health` local route
- `/version` local route
- Mobile narrow layout

## Safety boundary

Browser checks stay local. No service, cron, timer, tmux, Caddy, DNS, Discord, secret, global config, private source folder, real document, upload, OCR, indexing, vector store, or queue-015 change is made.
