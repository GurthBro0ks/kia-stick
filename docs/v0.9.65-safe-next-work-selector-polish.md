# KIA-Stick-v0.9.65-safe-next-work-selector-polish

Target machine: `USER_LAPTOP_ONLY`

## Safe Choices

When there are no actionable queue items, safe choices are limited to:

1. Continue fake-only proof/report/operator UX polish.
2. Repeat official-source research later if evidence changes.
3. Request exact Next target approval only if a clean target is proven.
4. Keep the real-doc gate blocked unless a separate one-document, one-gate approval packet is explicitly approved.

## Required Separations

- Fake-only proof/report/operator UX polish may touch docs, tests, helper output, and synthetic status surfaces.
- Official-source research must be read-only and must not mutate package files.
- Exact Next target work requires separate operator approval and must not start from this selector.
- Real-doc gate work requires a separate one-document, one-gate packet and must not start from this selector.

## Stop Conditions

Stop before work if a proposed next step would add a file picker, FileReader, browser File object handling, directory picker, drag/drop intake, path reader, upload handler, OCR, real redaction, text extraction over real documents, embeddings, indexing, vector store, private source inspection, dependency mutation, package-file mutation, service change, Discord notification, Caddy/DNS/cron/systemd/tmux change, or push.

## Boundary

This phase is fake-only docs/tests/tooling/status work. It does not approve implementation of Next/PostCSS, real documents, queue-015, v0.9.12C, package changes, runtime intake, services, notifications, or private-source access.
