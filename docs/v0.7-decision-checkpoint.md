# KIA Stick v0.7 Decision Checkpoint

Phase: `KIA-Stick-v0.6.7-backlog-closeout-v0.7-decision-checkpoint`

Status: `PLAN ONLY`

Baseline: `origin/main` verified at `6bbd6ce`, the pushed v0.6.6 future implementation gate draft.

Product version impact: none. `productVersion` stays `0.4.0`; any future product milestone bump must be approved in a separate implementation phase. This checkpoint only documents future bump options.

Model recommendation: Claude for the safety closeout and decision wording. GPT/Codex is acceptable for docs and tests. GLM-5.2 is optional for a second-opinion review only.

## Purpose

This document closes out the seeded v0.6.x planning backlog and presents an operator decision checkpoint for v0.7. It summarizes the current safe state, lists the v0.7 choices, and states the hard requirements for any real-doc path. It does not start any of those paths.

## Current Safe State

- **Fake app.** A laptop-only fake-doc PWA with citation-first governance, source hierarchy, conversational UX, and a hardened fake-vault/import-wizard scaffold. `productVersion` is `0.4.0`.
- **Proof tooling.** Local phase runner, proof index, and acceptance helpers produce GitHub-safe proof using labels, counts, booleans, and PASS/WARN/FAIL states only.
- **Queue tooling.** A local task queue (`docs/phase-backlog.json`) with status history, a Codex-ready `queue:next`, and a safety guard that rejects private paths and secrets-looking values.
- **v0.6 planning artifacts (docs/tests only, no implementation):**
  - `docs/v0.6-real-doc-pilot-plan.md` — real-doc pilot plan (PLAN ONLY).
  - `docs/v0.6-real-doc-safety-checklist.md` — pre-content safety gate checklist.
  - `docs/v0.6-operator-approval-packet.md` — copy-ready operator approval packet template.
  - `docs/v0.6-local-redaction-policy-plan.md` — local-only redaction review policy.
  - `docs/v0.6-future-implementation-gate-draft.md` — one-gate/one-document future prompt shape.
- **No real-doc implementation.** No file pickers, path readers, file reads, copying, OCR, real redaction, embeddings, indexing, vector stores, upload handlers, or private-vault inspection exist. The real-document mount and the private vault remain untouched and out of scope.

## Backlog Closeout

The seeded v0.6.x planning backlog (`queue-006` through `queue-010`) is complete and all docs/tests only:

- `queue-006` safety review checklist — accepted.
- `queue-007` fake-only pilot simulator — accepted.
- `queue-008` operator approval packet — accepted.
- `queue-009` local redaction policy plan — accepted.
- `queue-010` future implementation gate draft — accepted at this checkpoint after the pushed v0.6.6 baseline `6bbd6ce` was verified.

## v0.7 Choices

The operator may choose one of the following next directions. Each is a separate, future-approved phase; this checkpoint does not start any of them.

1. **Pause / stabilize.** No new features. Maintain the fake app, proof tooling, and queue tooling; address only small fixes. Lowest risk.
2. **Product-version bump plan.** Document a `productVersion` bump path (for example `0.4.0` toward a `0.5.0`/`0.7.0` milestone) as docs only. No code bump until separately approved. Low risk.
3. **Fake-only UX polish.** Improve the existing fake app UX with no real-doc handling and no new privacy surface. Low risk.
4. **Real-doc gate preparation.** Continue docs/tests-only preparation for a future single real-doc gate. Remains blocked from any content touch until separately approved. High risk; planning only.
5. **First real-doc gate request.** A future, separately approved prompt that requests exactly one gate for exactly one document. **Blocked** until all requirements below are met.

## Requirements For Any Real-Doc Path

No real-doc path may proceed unless every requirement below is met by a future, separately approved prompt:

- A completed, signed operator approval packet from `docs/v0.6-operator-approval-packet.md`, fresh and tied to the exact phase.
- A `PASS` result for `docs/v0.6-real-doc-safety-checklist.md` against the exact scope.
- A redaction policy result per `docs/v0.6-local-redaction-policy-plan.md` (PASS/WARN/FAIL by category, no unresolved WARN/FAIL).
- Conformance to `docs/v0.6-future-implementation-gate-draft.md`, naming **exactly one gate** and **exactly one document**.
- Fresh operator approval; no inherited, implied, or pasted approval from a prior run.
- Written rollback, deletion/retention, and GitHub-safe proof rules before any content-touching command.

Until all of the above are satisfied in a separately approved prompt, real-doc implementation is **blocked**.

## Next Safe Options In The Backlog

`docs/phase-backlog.json` is refreshed with planned v0.7 options (pause/stabilize, product-version bump plan, fake-only UX polish, and real-doc gate preparation). The first real-doc gate request remains a `blocked` backlog item until separately approved. No backlog item authorizes real-doc implementation.

## GitHub-Safe Proof Rules

Allowed proof fields:

- Phase name.
- Commit SHA.
- Command names.
- PASS/WARN/FAIL states.
- Gate names and outcome labels.
- Counts by safe category.
- Opaque pilot ID.
- Booleans such as `real_document_access=false` for planning phases.

Forbidden proof fields:

- Raw document text.
- Snippets.
- Private paths.
- Filenames when identifying.
- Member, employee, witness, officer, case, account, session, device, contact, or credential identifiers.
- OCR text.
- Hash values when sensitive.
- Exports.
- Backups.
- Private databases.
- Vector data.
- Embeddings.
- Private notes.
- Screenshots of real content.
- Any field that lets a reviewer reconstruct or locate the private source.

## What This Checkpoint Does Not Authorize

This checkpoint does not approve real detection, real redaction, real import, file pickers, path readers, file reads, copying, OCR, text extraction, summarization, transforms, embeddings, indexing, vector stores, upload handlers, private-vault inspection, cloud calls, service changes, pushes beyond this docs/test/state commit, or private document access.

## Required Tests And Checks

This phase must be covered by docs/tests proving:

- The checkpoint says `PLAN ONLY`.
- The checkpoint summarizes the current safe state: fake app, proof tooling, queue tooling, v0.6 planning artifacts, and no real-doc implementation.
- The checkpoint lists the v0.7 choices: pause/stabilize, product-version bump plan, fake-only UX polish, real-doc gate preparation, and first real-doc gate request.
- The checkpoint states the real-doc requirements: signed approval packet, PASS safety checklist, redaction policy, future gate draft, exactly one gate, exactly one document, and fresh operator approval.
- The checkpoint does not add file input, file picker, path reader, OCR, upload handling, vector store, or real indexing.
- The checkpoint has no raw private source path.
- The backlog refresh leaves real-doc implementation blocked until separately approved.
- Runtime surfaces still have no browser file input.
- No real-doc implementation path is added by this phase.

## Manual Operator Review

- Review this checkpoint.
- Review the v0.6 planning artifacts listed above.
- Review `npm run proof:latest`.
- Review `npm run closeout:summary`.
- Review `npm run queue:next`.
- Confirm the current phase accessed no real documents.
- Choose a v0.7 direction.
- For any real-doc path, confirm all real-doc requirements before any content-touching command.

## Current Phase Result

This phase is planning/state only. The intended result is one local docs/test/state commit after validation passes, then an auto-push of that commit only if every gate is `PASS`, no unrelated dirty files exist, no secrets or private paths are present, and only the expected ahead-by-one state remains. It must not access real documents.
