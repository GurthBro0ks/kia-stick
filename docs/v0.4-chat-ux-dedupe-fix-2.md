# KIA Stick v0.4 Chat UX Dedupe Fix 2

Phase: `KIA-Stick-v0.4-chat-ux-dedupe-fix-2`

Scope: fake-doc/fake-metadata-only manual QA blocker fix for saved-answer dedupe, chat-first composition, collapse-first details, source hierarchy coverage, and web manifest validity.

## Implemented

- Saved-answer identity now canonicalizes citation locators and ignores timestamp, build/display version, Git SHA, and citation/detail ordering noise.
- Legacy localStorage migration collapses timestamp/build-only duplicates while keeping the newest safe metadata.
- Same unchanged save still returns `Already saved. No new data.` without increasing saved count.
- Same-chat changed detail or metadata still replaces the existing saved card.
- Chat composer is message-first; response options and prompt shortcuts are secondary collapsed controls.
- Full packet detail sections are collapse-first: authority stack, conflicts, evidence checklist, missing facts, and follow-ups render behind disclosure controls.
- Sources hierarchy tests now verify expected fake docs under Local, State/Area, National, Manuals/Handbooks, Arbitration/Settlements, Steward Notes/Evidence, and Unknown.
- `public/manifest.webmanifest` is valid JSON and matches the app manifest metadata.

## Safety Boundary

- Real APWU/USPS/member/local/case/steward documents were not read, copied, listed recursively, scanned, OCRed, indexed, summarized, transformed, uploaded, ingested, or committed.
- `/media/mint/SHARED/APWU` and `~/kia-stick-private-vault` stayed out of scope.
- The implementation uses existing fake corpus metadata and deterministic local answer generation only.

## Validation Targets

- `npm run lint`
- `npm run typecheck`
- `npm run test`
- `npm run build`
- `npm run qa`
- `npm run scan:fake`
- `npm run scan:privacy`
- `node -e "JSON.parse(require('fs').readFileSync('public/manifest.webmanifest','utf8')); console.log('manifest json ok')"`
- Private tracked-path check for `DB/`, real document lanes, exports, backups, and vector-store paths.
