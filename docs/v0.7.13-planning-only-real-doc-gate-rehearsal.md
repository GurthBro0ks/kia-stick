# KIA Stick v0.7.13 Planning-Only Real-Doc Gate Rehearsal

Phase: `KIA-Stick-v0.7.13-planning-only-real-doc-gate-rehearsal`

Status: planning-only rehearsal packet for future operator review

Product version: `0.7.0`

Prompt version: `prompt.fake-docs.v0.5-import-wizard-hardening`

## Scope

This phase rehearses the operator workflow for a possible future one-document, one-gate approval. It uses fake labels, synthetic packet IDs, checklist fields, and PASS/WARN/FAIL outcomes only.

This phase is not an approval packet. It is **not** an approval packet. It does **not** authorize or implement real-doc work. It does not name, read, scan, upload, copy, hash, OCR, index, summarize, redact, inspect, transform, or touch any real document or private source folder.

## Hard Non-Approval

- No implementation is approved.
- No real document may be named or touched in this phase.
- No file picker, directory picker, drag/drop import zone, path reader, real upload, OCR, real redaction, text extraction, summarization, embeddings, indexing, vector store, upload handler, quarantine write, cloud/API call, service change, secret handling, Discord action, skill install, global agent config change, or queue-015 unblock is approved.
- `queue-015-v07-first-real-doc-gate-request` remains blocked.

## Synthetic-Only Rehearsal Packet

| Field | Synthetic value | Rule |
| --- | --- | --- |
| Rehearsal packet ID | `FAKE-GATE-REHEARSAL-001` | Fake ID only; no real approval packet exists. |
| Future document placeholder | `SYNTH-ONE-DOC-ALPHA` | Placeholder only; not a filename, path, hash, or document title. |
| Future gate placeholder | `SYNTH-GATE-METADATA-ONLY` | Placeholder only; not implementation approval. |
| Operator signoff placeholder | `SYNTH-OPERATOR-SIGNOFF-PENDING` | Rehearsal label only; no real signoff is recorded. |
| Proof output placeholder | `SYNTH-PROOF-SAFE-SUMMARY` | Counts, booleans, labels, commands, and PASS/WARN/FAIL only. |

The rehearsal packet must stay synthetic. If a field would require a real path, private storage reference, real filename, real document title, content excerpt, identifier, hash, OCR text, snippet, upload, or source-path value, the rehearsal result is `FAIL`.

## Exactly-One-Document And Exactly-One-Gate Future Rule

Any future real-doc prompt must separately pass both rules before work begins:

- `exactly-one-document`: the operator names exactly one future document in a separate approval packet.
- `exactly-one-gate`: the operator names exactly one narrow future gate in the same approval packet.
- The prompt cannot include broad source scope, recursive scope, private-source browsing, source-path language, directory-level language, batch language, wildcard language, or "scan everything" language.
- The allowed action must be narrow, reversible, and written before any content touch.
- Blocked actions must be listed before the gate starts.
- Proof-safe output rules must be accepted before the gate starts.
- Stop conditions must be accepted before the gate starts.

This v0.7.13 rehearsal records only the checklist shape. It does not approve the future document or future gate.

## Future Operator Approval Packet Checklist

A future approval packet must include all of these fields before any later implementation prompt can proceed:

- Exact future phase name.
- Exactly one future document scope.
- Exactly one future gate action.
- Explicit allowed action.
- Explicit blocked actions.
- Operator signoff handle.
- Approval date and time.
- Safety checklist result.
- Redaction/privacy policy result.
- Rollback plan.
- Deletion and retention plan.
- Proof-safe output agreement.
- Stop-on-WARN/FAIL agreement.
- Confirmation that no broad/private/recursive/source-path wording is present.
- Confirmation that `queue-015` is still blocked until a separate approved prompt explicitly changes it.

Missing, ambiguous, broad, or path-shaped fields are `FAIL`.

## Future Stop-On-WARN/FAIL Checklist

Use these labels during future packet review:

- `PASS`: The packet is synthetic or separately operator-approved, names exactly one future document and exactly one future gate, lists blocked actions, confirms no broad/private/recursive/source-path wording, and contains proof-safe output rules before work starts.
- `WARN`: The packet is still synthetic but has ambiguous wording, missing reviewer fields, unclear rollback/deletion language, incomplete proof-safe output rules, or unclear stop conditions. Stop and revise before any future implementation.
- `FAIL`: The packet names or requests broad/private/recursive/source-path work, asks for real document access in this rehearsal, asks for a directory, asks for batch handling, requests file reads/uploads/OCR/indexing/vector work, or implies queue-015 is unblocked. Stop and do not proceed.

Any `WARN` or `FAIL` blocks future real-doc implementation until a separate corrected packet is reviewed.

## Redaction And Privacy Policy Placeholders

- Redaction reviewer role: `TBD in a future approval packet`.
- Privacy reviewer role: `TBD in a future approval packet`.
- Default decision on uncertainty: `FAIL / do not proceed`.
- Index eligibility decision: `blocked until metadata and redaction policy pass`.
- Retention/deletion rule: `TBD in a future approval packet`.
- Proof detail level: `metadata and guard fields only`.

These placeholders do not authorize redaction, OCR, content extraction, hashing, indexing, embeddings, or vector storage.

## Proof-Safe Output Rules

Allowed proof output:

- Phase names.
- Command names.
- Queue IDs and statuses.
- Product and prompt versions.
- Synthetic IDs.
- Booleans and counts.
- PASS/WARN/FAIL labels.
- Guard fields such as `realDocumentAccessed=false`, `filePickerAdded=false`, and `queue015Status=blocked`.

Forbidden proof output:

- Real document text.
- Real snippets.
- Real filenames.
- Private source folder names.
- Real paths.
- Account/session data.
- Secrets or credentials.
- Hashes from sensitive material.
- OCR output.
- Embeddings or vector data.
- Upload payloads.
- Private notes.

## Rehearsal PASS/WARN/FAIL Examples

- `PASS`: `FAKE-GATE-REHEARSAL-001` includes `SYNTH-ONE-DOC-ALPHA`, `SYNTH-GATE-METADATA-ONLY`, blocked actions, stop conditions, proof-safe output rules, and `queue015Status=blocked`.
- `WARN`: The synthetic packet is missing the deletion/retention placeholder or uses unclear reviewer role text, but it still does not request real document access.
- `FAIL`: The packet asks for a folder, broad source review, recursive scan, source-path intake, file upload, OCR, indexing, vector storage, or queue-015 unblocking.

## Required Closeout Gates Before Any Future Real-Doc Implementation

Before any later implementation prompt can be considered, the operator must complete a separate closeout showing:

- This v0.7.13 rehearsal validated with PASS.
- The future approval packet exists and is separately approved.
- The future safety checklist is PASS.
- The future redaction/privacy policy result is PASS.
- Exactly one future document and exactly one future gate are named.
- `queue-015` remains blocked until the separate implementation prompt explicitly requests and proves a safe transition.
- No real document was touched during planning.
- Proof output stays GitHub-safe.
- Product version remains `0.7.0`.
- Prompt version remains `prompt.fake-docs.v0.5-import-wizard-hardening`.

## Queue State

- `queue-028-v0713-planning-only-real-doc-gate-rehearsal` records this rehearsal for operator review.
- `queue-015-v07-first-real-doc-gate-request` remains blocked.
- v0.7.13 does not unblock queue-015.

## Validation Expectations

Expected validation outcome for this phase:

- v0.7.13 remains planning-only.
- No real-doc work is authorized.
- The future rule requires exactly one gate and exactly one document.
- Broad/private/recursive/source-path language is rejected.
- `queue-015` remains blocked.
- `productVersion` remains `0.7.0`.
- `promptVersion` remains `prompt.fake-docs.v0.5-import-wizard-hardening`.
- No file picker, path reader, OCR, indexing, vector, upload, or real-doc capability is added.

## Clear Answer

v0.7.13 rehearses the future operator gate shape with synthetic placeholders only. It creates no real-doc approval packet and approves no implementation.

## Confidence Level

`0.90`

## Key Caveats

- This phase is only a rehearsal.
- A future implementation still needs a separate operator-approved packet with exactly one gate and exactly one document.
- Any `WARN` or `FAIL` stops the future gate path.
