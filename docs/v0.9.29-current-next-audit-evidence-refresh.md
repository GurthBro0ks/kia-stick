# KIA-Stick-v0.9.29-current-next-audit-evidence-refresh

Target machine: `USER_LAPTOP_ONLY`

## Scope

This phase records read-only npm package, audit, and registry metadata for the parked Next/PostCSS runtime security path. It does not install, update, dedupe, prune, audit-fix, or mutate package manifests.

## Commands Captured

- `npm ls next react react-dom postcss --all`
- `npm audit --json`
- `npm view next version`
- `npm view next dist-tags --json`
- `npm view next@latest version peerDependencies dependencies --json`
- `npm view react version`
- `npm view react-dom version`
- `npm view postcss version`

Network metadata status: `PASS`.

## Current Package State

- `package.json` direct `next`: `^15.1.3`.
- Lockfile `next`: `15.5.19`.
- Lockfile `react`: `19.2.7`.
- Lockfile `react-dom`: `19.2.7`.
- Next transitive `postcss`: `8.4.31`.
- Vite nested `postcss`: `8.5.15`.
- npm registry `next` latest: `16.2.9`.
- npm registry `react` latest: `19.2.7`.
- npm registry `react-dom` latest: `19.2.7`.
- npm registry `postcss` latest: `8.5.15`.

## Audit Evidence

- `npm audit --json` exited with status `1`.
- Audit vulnerability count: `2 moderate`.
- Direct affected package: `next`.
- Transitive affected package: `postcss`.
- `next` audit path: direct dependency via `postcss`.
- `next` affected range: `9.3.4-canary.0 - 16.3.0-canary.5`.
- `postcss` advisory: `GHSA-qx2v-qp2m-jg93`.
- `postcss` affected range: `<8.5.10`.
- npm audit suggested fix: `next@9.3.3`, semver-major forced downgrade.

## Latest Next Metadata

`next@latest` is `16.2.9`. Its registry metadata lists peer compatibility for React `^18.2.0`, a React 19 RC, or `^19.0.0`, so the current React `19.2.7` line is compatible at the peer range level.

`next@latest` metadata still lists dependency `postcss: 8.4.31`, which is inside the npm audit affected range `<8.5.10`. Because this phase does not install and run an audit against a mutated lockfile, `next@16.2.9` is `not_proven_clean`.

## Result

Result: `WARN_SAFE_NEXT_TARGET_UNCLEAR`.

No exact clean target is proven by this evidence. `KIA-Stick-v0.9.12C-next-runtime-framework-security-implementation` remains blocked pending a separate operator-approved exact target and an implementation proof.

## Safety

- No `npm install`, `npm update`, `npm audit fix`, `npm dedupe`, or `npm prune` was run.
- `package.json` and `package-lock.json` remain unchanged.
- Product/package version remains `0.7.0`.
- Prompt version remains `prompt.fake-docs.v0.5-import-wizard-hardening`.
- `queue-015-v07-first-real-doc-gate-request` remains blocked.
- No real-doc capability is added.
