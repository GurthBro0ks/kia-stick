# KIA-Stick-v0.8.8-sources-upload-import-vault-polish

Product version: `0.7.0`
Prompt version: `prompt.fake-docs.v0.5-import-wizard-hardening`
Queue item: `queue-043-v088-sources-upload-import-vault-polish`

## Scope

This subphase improves fake-only scan density and operator labels in Sources, Upload, Import, and Vault.

Allowed runtime files touched: `components/KiaStickApp.tsx` and `app/globals.css`.

## Runtime polish

- Sources summary now reports total fake sources, citable sources, context-only guardrails, prompt version, and build identity.
- Each Sources hierarchy card now includes citable/context-only counts before the fake document rows.
- Upload now includes a QA cue rail showing metadata buttons only, no file chooser, and queued fake row count.
- Vault now includes a QA cue rail showing fake metadata row count, redaction review count, metadata review count, and index-gate status.
- Import now includes a QA cue rail showing synthetic-candidate-only status, current fake step, fake index decision, and disabled real actions.
- Narrow CSS now wraps QA cue rails and bottom nav labels without changing the fake state machines.

## Preserved behavior

- Upload remains fake metadata buttons only.
- Import remains a fake metadata state machine only.
- Vault remains fake metadata and governance only.
- Sources remain a fictional fake-doc hierarchy only.
- Audit exports remain fake metadata, guard flags, and build identity only.

## Safety boundary

This subphase does not add file pickers, directory pickers, drag/drop intake, FileReader, browser File object handling, path readers, upload handlers, OCR, real redaction, text extraction over real documents, embeddings, indexing, vector stores, private source inspection, queue-015 unblocking, service changes, Discord, or secrets.
