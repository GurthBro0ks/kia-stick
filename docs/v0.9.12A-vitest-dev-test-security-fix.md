# KIA-Stick-v0.9.12A-vitest-dev-test-security-fix

Target machine: `USER_LAPTOP_ONLY`

Accepted baseline commit: `915b819cf9fdd9e688c62c210bf523c5e34741a0`

Proof directory: `/home/mint/kia-stick-local-proofs/proof_kia_stick_v0_9_12A_vitest_dev_test_security_fix_20260627T153005Z`

## Scope

This phase fixes the Vitest dev/test dependency security path only. It does not change the Next/runtime dependency path, runtime code, product version, prompt version, queue-015 state, or real-document capability.

No `npm audit fix`, `npm update`, Next upgrade, direct `postcss` change, runtime feature work, service change, Discord send, or push is approved by this phase.

## Targeted Package Change

The accepted v0.9.11 triage found a direct critical `vitest` advisory and transitive dev/test advisories through `vite`, `vite-node`, `@vitest/mocker`, and `esbuild`.

Current state before this phase:

- Direct declaration: `vitest` `^2.1.8`
- Installed lockfile version: `vitest@2.1.9`
- Dev/test chain: `vite@5.4.21`, `vite-node@2.1.9`, `@vitest/mocker@2.1.9`, `esbuild@0.21.5`

Targeted command used:

```bash
npm install --save-dev vitest@3.2.6
```

Resulting state:

- Direct declaration: `vitest` `^3.2.6`
- Installed lockfile version: `vitest@3.2.6`
- Dev/test chain: `vite@7.3.6`, `vite-node@3.2.4`, `@vitest/mocker@3.2.6`, `esbuild@0.28.1`

`vitest@3.2.6` was selected because npm audit reported the direct vulnerable range as `<=3.2.5`, and npm registry metadata identified `3.2.6` as the maintained `V3` dist-tag. This is a major upgrade from Vitest 2 to Vitest 3, but it is smaller than the Vitest 4 latest path and keeps the change in dev/test tooling.

## Audit Result

Before this phase, npm audit reported the Vitest dev/test chain:

| Package | Before severity |
|---|---|
| `vitest` | critical |
| `vite` | high |
| `vite-node` | moderate |
| `@vitest/mocker` | moderate |
| `esbuild` | moderate |

After the targeted Vitest update, npm audit reports no remaining `vitest`, `vite`, `vite-node`, `@vitest/mocker`, or `esbuild` vulnerability entries.

Remaining audit work is limited to the deferred Next/runtime path:

- `next`
- `postcss`

That runtime framework work is deferred to `KIA-Stick-v0.9.12B-next-runtime-framework-security-plan`.

## Runtime Guard

The package-lock runtime guard compared the before/after versions for:

- `node_modules/next`
- `node_modules/react`
- `node_modules/react-dom`
- `node_modules/postcss`

All four remained unchanged. The Next/runtime dependency path was not changed intentionally or as a side effect of the Vitest update.

## State Guarantees

- `package.json` changed only to update direct dev dependency `vitest` to `^3.2.6`.
- `package-lock.json` changed only through the targeted Vitest dev/test dependency update.
- No runtime source files were changed.
- No Next/runtime package was changed.
- No direct `postcss` change was made.
- No `npm audit fix` was run.
- No `npm update` was run.
- Product/package version remain `0.7.0`.
- Prompt version remains `prompt.fake-docs.v0.5-import-wizard-hardening`.
- `queue-015-v07-first-real-doc-gate-request` remains blocked.
- No real-doc implementation is approved.
- No file picker, directory picker, drag/drop import zone, browser File object, FileReader, path reader, upload handler, OCR, text extraction, summarization, embeddings, indexing, vector store, private-source inspection, service change, Discord send, secret handling, or system change is approved.
- Push is not performed in this implementation phase.

## Next Step

Operator review is required before closeout/push. The next separate security phase should be `KIA-Stick-v0.9.12B-next-runtime-framework-security-plan` for the Next/runtime framework path.
