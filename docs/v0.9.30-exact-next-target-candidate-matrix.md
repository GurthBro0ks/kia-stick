# KIA-Stick-v0.9.30-exact-next-target-candidate-matrix

Target machine: `USER_LAPTOP_ONLY`

## Scope

This docs-only matrix compares exact Next target candidates using captured npm audit and npm view evidence. It does not approve any target for implementation.

## Candidate Matrix

| Candidate | Evidence source | Command shape only | Peer / React considerations | PostCSS relationship | Risk | Unknowns | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `next@15.5.19` current lockfile | `npm ls`, `npm audit --json` | no command; current state only | Current React `19.2.7` works in current app | Uses `postcss@8.4.31`; audit reports `postcss <8.5.10` and direct `next` via `postcss` | medium | No package mutation allowed here | `blocked_current_audit_warn` |
| `next@16.2.9` latest | `npm view next version`, `npm view next@latest ... --json` | `npm install --save-exact next@16.2.9` | Peer range includes `^19.0.0`, compatible with React `19.2.7` at metadata level | Registry metadata lists `postcss: 8.4.31`, still inside `<8.5.10`; not audited in a mutated lockfile | high | Build/runtime migration risk and audit result after install are unknown | `not_proven_clean` |
| `next@16.2.8` backport tag | `npm view next dist-tags --json` | `npm install --save-exact next@16.2.8` | Needs separate metadata and install proof; not checked beyond dist-tag | PostCSS dependency unknown in this phase | high | Exact dependency tree and audit result unknown | `not_proven_clean` |
| `next@15.3.9` next-15-3 tag | `npm view next dist-tags --json` | `npm install --save-exact next@15.3.9` | Same major line as current, but older than current lockfile `15.5.19` | Not proven to clear PostCSS advisory | high | Exact dependency tree and audit result unknown | `blocked_not_upgrade` |
| `next@9.3.3` npm audit suggested fix | `npm audit --json` | `npm install --save-exact next@9.3.3` | Not compatible with the current React/Next 15 app contract without major migration risk | npm audit marks as fixAvailable, but it is a semver-major forced downgrade | critical | Runtime breakage risk is unacceptable without a migration plan | `blocked_forced_downgrade` |

## Interpretation

No candidate is `proven_clean`. The latest exact target `next@16.2.9` is a plausible future research candidate only, because npm metadata shows React 19 peer compatibility but also lists `postcss@8.4.31`, which remains inside the advisory range from the audit evidence.

## Decision

Result: `WARN_SAFE_NEXT_TARGET_UNCLEAR`.

This phase does not approve `next@16.2.9`, `next@16.2.8`, `next@15.3.9`, `next@9.3.3`, a direct `postcss` override, or any implementation command. A future implementation needs a separate operator-approved exact target, package-lock review, full validation, and rollback plan.

## Blocks Preserved

- `KIA-Stick-v0.9.12C-next-runtime-framework-security-implementation` remains blocked.
- `queue-015-v07-first-real-doc-gate-request` remains blocked.
- Next/PostCSS remains parked as `WARN_SAFE_NEXT_TARGET_UNCLEAR`.
- Product/package version remains `0.7.0`.
- Prompt version remains `prompt.fake-docs.v0.5-import-wizard-hardening`.
- No real-doc capability is added.
