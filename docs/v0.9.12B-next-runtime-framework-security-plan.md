# KIA-Stick-v0.9.12B-next-runtime-framework-security-plan

Target machine: `USER_LAPTOP_ONLY`

Accepted pushed baseline commit: `f412bcf8e802f3ef0a800d46a0ab6b32da7f4da3`

Proof directory: `/home/mint/kia-stick-local-proofs/proof_kia_stick_v0_9_12B_next_runtime_framework_security_plan_20260627T155320Z`

## Scope

This phase is planning-only dependency/security work for the remaining Next/runtime framework audit path after the accepted v0.9.12A Vitest dev/test fix.

It inspects and records the current `next` and transitive `postcss` advisories, identifies the risk areas for a future Next implementation phase, and defines the gate for that future phase. It does not implement a Next upgrade.

No `npm audit fix`, `npm update`, `npm install`, package-manager mutation, runtime source edit, runtime capability change, product version bump, prompt version bump, queue-015 unblock, or real-doc capability is approved by this plan.

## Current Runtime Dependency State

`package.json` declares:

- `next`: `^15.1.3`
- `react`: `^19.0.0`
- `react-dom`: `^19.0.0`

`package-lock.json` resolves:

- `next`: `15.5.19`
- `react`: `19.2.7`
- `react-dom`: `19.2.7`
- top-level `postcss`: `8.4.31`
- nested `node_modules/vite/node_modules/postcss`: `8.5.15`

The nested Vite `postcss@8.5.15` path is already on the dev/test side after v0.9.12A. The remaining runtime audit issue is the Next dependency path using top-level `postcss@8.4.31`.

## Audit Summary

`npm audit --json` reported 2 remaining moderate vulnerable package entries:

- `next`: direct runtime/build framework dependency.
- `postcss`: transitive dependency affected through `next`.

The audit fix suggestion is not safe to apply as-is. It reports a forced semver-major fix to `next@9.3.3`, which would be a major downgrade from the current Next 15 lockfile state and is not a credible safe runtime framework fix.

## Candidate Evidence

Registry metadata checked with `npm view` showed:

- `next@15.5.19` is the current lockfile version and still depends on `postcss@8.4.31`.
- latest stable `next` dist-tag is `16.2.9`.
- current canary dist-tag is `16.3.0-canary.69`.
- `next@16.0.10` and `next@16.3.0-canary.5` metadata still show `postcss: 8.4.31`.
- Next 15 and Next 16 peer ranges include React 19.
- Next 16 metadata requires Node `>=20.9.0`.
- latest stable `react` is `19.2.7`.
- latest stable `postcss` is `8.5.15`.

The safe target remains unclear from `npm audit` and `npm view` alone because the audit advisory range still includes current stable/candidate Next versions through `16.3.0-canary.5`, while the audit fix suggestion points to an unsafe forced downgrade. A future implementation phase must select an operator-approved Next version only after advisory-specific confirmation.

## Risk Matrix

| Area | Risk | Required future proof |
|---|---|---|
| Next runtime framework upgrade | Medium/high because Next controls routing, build, rendering, and server behavior. | Package diff, lint, typecheck, full tests, build, `/health`, `/version`, desktop and mobile browser smoke. |
| Transitive `postcss` through Next | Medium because the vulnerable path is resolved by Next, not this app directly. | Confirm audit result after the chosen Next version; avoid manual direct `postcss` override unless separately approved. |
| React 19 compatibility | Medium. Current React 19.2.7 satisfies Next 15/16 peer metadata, but runtime behavior still needs proof. | Verify package tree, build, and browser smoke across Chat, Sources, Saved, Upload, Import, Vault, and Settings. |
| Node/deployment compatibility | Medium. Next 16 requires Node `>=20.9.0`. | Capture local and target Node versions before implementation; stop if deployment/runtime Node is below the selected Next engine floor. |
| App Router/rendering behavior | Medium. Framework changes can alter static/dynamic rendering and route behavior without app source edits. | Build output review plus `/health` and `/version` smoke after production build. |
| CSS/PostCSS pipeline | Medium. Styling may shift if framework compilation changes. | Desktop and narrow/mobile screenshots for main surfaces; scan for layout regressions. |
| Security audit ambiguity | High. `npm audit` does not currently prove a clean stable Next target. | Stop with WARN unless advisory-specific evidence shows the chosen version clears `next` and `postcss`. |
| Rollback | Low if package-only implementation remains isolated. | Use normal `git revert` of the implementation commit; do not use destructive reset. |

## Recommended Implementation Gate

Recommended next phase:

`KIA-Stick-v0.9.12C-next-runtime-framework-security-implementation`

That phase may run a package-manager command only after the operator approves a specific Next target. The preferred command shape is:

```bash
npm install --save-exact next@<operator-approved-version>
```

Do not add a direct `postcss` dependency or override unless a separate operator-approved plan requires it.

Expected implementation files are limited to `package.json`, `package-lock.json`, dependency-security documentation/state, and focused tests unless the selected Next version forces explicit source compatibility fixes. If runtime source changes become necessary, stop and split that into a separate operator-approved runtime compatibility phase.

Mandatory validation for the future implementation phase:

- `npm audit --json`
- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm run release:check`
- `npm run governance:report`
- `npm run queue:next`
- `npm run closeout:summary`
- `npm run scan:fake`
- `npm run scan:privacy`
- `git diff --check`
- package diff and package hash proof before/after

Mandatory browser smoke surfaces after a successful build:

- Chat
- Sources
- Saved
- Upload
- Import
- Vault
- Settings
- `/version`
- `/health`
- desktop viewport
- mobile/narrow viewport

Future implementation stop conditions:

- selected Next target remains audit-ambiguous or still reports the same `next`/`postcss` advisories.
- `npm audit` suggests a forced downgrade or unrelated dependency rewrite.
- Next upgrade requires broad runtime source changes.
- lint, typecheck, tests, build, release check, governance report, queue check, or browser smoke fails.
- package movement extends beyond the approved Next dependency path.
- product version, prompt version, `queue-015`, real-doc gate, or fake-only boundary drifts.
- file picker, directory picker, drag/drop import zone, browser File object, FileReader, path reader, upload handler, OCR, text extraction, summarization, embeddings, indexing, vector store, private-source inspection, service, secret, Discord, or system-change capability appears.

## State Guarantees

- `package.json` was not changed.
- `package-lock.json` was not changed.
- No dependency versions were changed.
- No runtime code was changed.
- Product/package version remain `0.7.0`.
- Prompt version remains `prompt.fake-docs.v0.5-import-wizard-hardening`.
- `queue-015-v07-first-real-doc-gate-request` remains blocked.
- No real-doc implementation is approved.
- No file picker, directory picker, drag/drop import zone, browser File object, FileReader, path reader, upload handler, OCR, text extraction, summarization, embeddings, indexing, vector store, private-source inspection, service change, Discord send, secret handling, or system change is approved.

## Result

Planning output is complete, but the safe Next target is unclear from current `npm audit` and `npm view` evidence.

Result: `WARN_SAFE_NEXT_TARGET_UNCLEAR`
