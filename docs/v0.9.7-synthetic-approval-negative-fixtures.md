# KIA-Stick-v0.9.7-synthetic-approval-negative-fixtures

Product version: `0.7.0`
Prompt version: `prompt.fake-docs.v0.5-import-wizard-hardening`
Queue item: `queue-052-v097-synthetic-approval-negative-fixtures`

## Purpose

This phase expands synthetic negative and edge-case fixture coverage for future approval-packet review.

Fixtures remain in-memory synthetic objects only. The validator does not accept arbitrary path input, check filesystem existence, read user files, inspect folders, or name/touch real documents.

## Fixture Coverage

- Missing operator signature placeholder: `WARN`.
- Too-broad scope wording: `FAIL`.
- Recursive source wording: `FAIL`.
- Private/path-shaped wording: `FAIL`.
- Multiple future document placeholders: `FAIL`.
- Multiple future gate placeholders: `FAIL`.
- Missing rollback plan: `FAIL`.
- Missing deletion/retention placeholder: `WARN`.
- Missing redaction-policy result: `WARN`.
- Unsafe proof output agreement: `FAIL`.

The fixture matrix still preserves `PASS`, `WARN`, and `FAIL` semantics and keeps `queue-015-v07-first-real-doc-gate-request` blocked.

## Safety

The fixture module is data-only TypeScript. It does not import filesystem APIs, does not parse command-line file paths, and does not add file picker, upload, OCR, indexing, vector-store, or real-doc intake behavior.
