# KIA Stick v0.8.5 Next Large Work Checkpoint

Phase: `KIA-Stick-v0.8.5-next-large-work-checkpoint`

Status: next large-work checkpoint

Product version: `0.7.0`

Prompt version: `prompt.fake-docs.v0.5-import-wizard-hardening`

## Scope

This checkpoint records the next large safe work options after the v0.8.1 through v0.8.5 backlog reconciliation bundle.

It does not bump product version. It does not approve real-doc implementation. It does not unblock `queue-015-v07-first-real-doc-gate-request`.

## Recommended Large Work Options

1. Fake-only runtime UX polish bundle
   - Focus: Chat/Sources/Saved/Upload/Import/Vault/Settings scan density, operator labels, no-answer ergonomics, and mobile/narrow QA.
   - Boundary: fake sample data only; no file chooser, path reader, upload handler, OCR, indexing, or real-doc flow.
   - Suggested model: GPT/Codex for implementation, Claude for final safety review.

2. Synthetic governance hardening bundle
   - Focus: additional synthetic negative fixtures, report snapshots, drift guards, and queue/proof consistency checks.
   - Boundary: built-in synthetic fixtures and repo-owned metadata only; no user-provided packet files or private-source inspection.
   - Suggested model: GPT/Codex for tooling/tests, optional GLM-5.2 medium review for long-context consistency.

3. Planning-only real-doc packet preparation
   - Focus: create a review packet template and checklist for a future exactly-one-document, exactly-one-gate request.
   - Boundary: planning-only; no real document access, no file reads, no paths, no OCR, no hashing, no redaction, no upload, no indexing, no vector storage, and no queue-015 unblock.
   - Suggested model: Claude for safety reasoning, GPT/Codex only for repo-local docs/tests after explicit approval.

4. Release-state consolidation bundle
   - Focus: closeout docs, feature state, queue state, proof index, and README/CLOSEOUT consistency after operator QA.
   - Boundary: no push unless separately approved after validation PASS and operator QA PASS.
   - Suggested model: Claude for final closeout review after Codex validation.

## Product Version Rule

`productVersion` remains `0.7.0`. A product-version bump requires a separate approved product-version phase and cannot be bundled into this checkpoint by implication.

## Queue Rule

The next large work should get fresh queue IDs after `queue-040` unless it is a closeout-only follow-up for this bundle. `queue-015-v07-first-real-doc-gate-request` remains blocked.

## Clear Answer

The recommended next bundle is fake-only runtime UX polish or synthetic governance hardening. Planning-only real-doc packet preparation is allowed only as planning, not implementation.

## Confidence Level

`0.88`

## Key Caveats

- Operator QA is still needed for the current v0.8.1 through v0.8.5 bundle.
- Real-doc implementation remains blocked.
- Product version stays `0.7.0` until separately approved.
