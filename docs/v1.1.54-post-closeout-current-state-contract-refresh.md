# KIA-Stick-v1.1.54-post-closeout-current-state-contract-refresh

- `data/current-accepted-pushed-state.json` records `v1.1.52 at 857ba0e` and its exact accepted closeout proof.
- `b8fb834` and older baselines are historical only.
- Future closeout pushes require a separate static fake-only refresh.
- The contract does not read proof directories at runtime.
