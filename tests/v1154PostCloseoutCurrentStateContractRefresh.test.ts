import { readFileSync } from "node:fs"; import { describe, expect, it } from "vitest";
describe("v1.1.54 contract refresh",()=>{it("keeps exact current and historical roles",()=>{const c=readFileSync("data/current-accepted-pushed-state.json","utf8"),d=readFileSync("docs/v1.1.54-post-closeout-current-state-contract-refresh.md","utf8");expect(c).toContain('"accepted_pushed_short_commit": "ab1878e"');expect(c).toContain('"short_commit": "b8fb834"');expect(c).not.toContain('"accepted_pushed_short_commit": "b8fb834"');expect(d).toContain("does not read proof directories at runtime");});});
