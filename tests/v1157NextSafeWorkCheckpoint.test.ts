import { readFileSync } from "node:fs"; import { describe, expect, it } from "vitest";
describe("v1.1.57 next safe work",()=>{it("keeps local and blocked states",()=>{const s=JSON.parse(readFileSync("feature_list.json","utf8")).v1157_next_safe_work_checkpoint;expect(s.current_accepted_pushed_short_commit).toBe("857ba0e");expect(s.local_checkpoint_pushed).toBe(false);expect(s.local_checkpoint_manual_qa_status).toBe("pending_operator_review");expect(s.next_postcss_status).toBe("WARN_SAFE_NEXT_TARGET_UNCLEAR");expect(s.v0912c_status).toBe("blocked_pending_exact_target");expect(s.queue_015_status).toBe("blocked");});});
